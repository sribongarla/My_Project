server:
  port: 8080

app:
  environment: ${APP_ENV:local}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    health:
      probes:
        enabled: true
  metrics:
    tags:
      application: eks-devops-platform
